import 'package:flutter/material.dart';
class ExifScreen extends StatelessWidget {
  const ExifScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('EXIF Privacy Wiped')), body: const Center(child: Text('EXIF Wiping Workspace')));
}
