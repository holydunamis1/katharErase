import 'package:flutter/material.dart';
class WatermarkScreen extends StatelessWidget {
  const WatermarkScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('Watermark Studio')), body: const Center(child: Text('Watermark Studio Workspace')));
}
